
$("#fav1").on('change', function() {

  var selected =  $("#fav1 option:selected").val();

  $('#fav2 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav3 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav4 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav5 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav6 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav7 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav8 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav9 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav10 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});

$("#fav2").on('change', function() {

  var selected =  $("#fav2 option:selected").val();

  $('#fav1 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav3 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav4 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav5 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav6 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav7 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });
  
  $('#fav8 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav9 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav10 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});

$("#fav3").on('change', function() {

  var selected =  $("#fav3 option:selected").val();

  $('#fav1 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav2 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav4 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav5 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

    $('#fav6 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav7 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });
  
  $('#fav8 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav9 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav10 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});

$("#fav4").on('change', function() {

  var selected =  $("#fav4 option:selected").val();

  $('#fav1 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav2 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav3 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav5 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

    $('#fav6 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav7 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });
  
  $('#fav8 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav9 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav10 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});

$("#fav5").on('change', function() {

  var selected =  $("#fav5 option:selected").val();

  $('#fav1 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav2 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav3 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav4 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

    $('#fav6 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav7 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });
  
  $('#fav8 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav9 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav10 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});


$("#fav6").on('change', function() {

  var selected =  $("#fav6 option:selected").val();

  $('#fav1 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav2 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav3 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav4 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

    $('#fav5 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav7 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });
  
  $('#fav8 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav9 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav10 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});


$("#fav7").on('change', function() {

  var selected =  $("#fav7 option:selected").val();

  $('#fav1 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav2 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav3 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav4 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

    $('#fav6 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav5 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });
  
  $('#fav8 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav9 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav10 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});


$("#fav8").on('change', function() {

  var selected =  $("#fav8 option:selected").val();

  $('#fav1 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav2 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav3 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav4 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

    $('#fav6 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav7 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });
  
  $('#fav5 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav9 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav10 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});


$("#fav9").on('change', function() {

  var selected =  $("#fav9 option:selected").val();

  $('#fav1 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav2 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav3 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav4 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

    $('#fav6 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav7 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });
  
  $('#fav8 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav5 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav10 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});


$("#fav10").on('change', function() {

  var selected =  $("#fav10 option:selected").val();

  $('#fav1 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav2 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav3 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav4 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

    $('#fav6 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav7 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });
  
  $('#fav8 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav9 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

  $('#fav5 option').each(function() {
    if ( $(this).val() === selected ) {
        $(this).remove();
    }
  });

});
